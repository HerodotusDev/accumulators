{
  "name": "merkle-mountain-range",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "postinstall": "yarn build",
    "test": "jest",
    "integration:start": "yarn --cwd ./integration start",
    "integration:dev": "yarn --cwd ./integration dev"
  },
  "workspaces": [
    "packages/*",
    "integration"
  ],
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^18.14.2",
    "jest": "^29.4.3",
    "lerna": "^6.5.1",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  }
}
